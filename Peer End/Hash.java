/**
 * @author CHOI Chong Hing (UID: 3035564940)
 * The Hash interface
 * It hashes and encrypts Strings
 */
public interface Hash{

	/**
	 * The hash() method
	 * @param input - The String to be encrypted and hashed
	 * @return the hashed String
	 */
	public String hash(String input);
	
}
